useEffect(() => {
    if (isAuthenticated) {
      Service.getUserSavedBooks(user.nickname).then((response) => {
        setSavedBooks({ saved: response.data });
      });
    }
  }, [isAuthenticated]);

  useEffect(() => {
    for (const element of savedBooks.saved) {
      Service.getBookById(element.book).then((res2) => {
        bookArray.push(res2.data);
      });
    }
    setSavedBookInfo(bookArray);
  }, [savedBooks.saved]);

  useEffect(() => {
    setLoading(false);
  }, [savedBookInfo]);